from django.apps import AppConfig

class InteroperabiliteConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'interoperabilite'
    verbose_name = 'Interopérabilité FHIR/HL7'
