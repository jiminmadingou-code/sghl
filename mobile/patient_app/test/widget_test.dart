import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test', (WidgetTester tester) async {
    // Test vide — l'app utilise go_router et secure storage non testables unitairement
    expect(true, isTrue);
  });
}
